name: 02-workflow-events

on: push

jobs:
    echo: 
        runs-on: ubuntu-latest
        steps:
            - name: show the trigger
              run: echo "i'm triggerd by ${{ github.event_name }}"