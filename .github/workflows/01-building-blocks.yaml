name: 01-build Block

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Custom Error Step
              run: |
                echo "This step will fail"
                exit 1  # 自定义错误
            - name: Say goodBye
              run: echo "Good bye"

            




    