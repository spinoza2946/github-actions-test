name: 01-build Block

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say Hello
              run: echo "Hello"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: 
                echo "I will fail"
                exit 1
            - name: Say goodBye
              if: success()  # 仅在前一步成功时执行
              run: echo "Good bye"

            




    